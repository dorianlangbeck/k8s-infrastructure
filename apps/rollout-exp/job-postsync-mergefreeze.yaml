apiVersion: batch/v1
kind: Job
metadata:
  name: mergefreeze-unfreeze
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
    argocd.argoproj.io/sync-wave: "100"

spec:
  template:
    spec:
      containers:
        - name: curl
          image: alpine/curl
          args:
            - -X
            - POST
            - https://www.mergefreeze.com/api/branches/dorianlangbeck/testing/main/?access_token=$(ACCESS_TOKEN)
            - -d
            - frozen=false&user_name=ArgoCD PostSync&note=Sync in completed
          env:
            - name: ACCESS_TOKEN
              valueFrom:
                secretKeyRef:
                  name: merge-freeze
                  key: access-token
      restartPolicy: Never
  backoffLimit: 2
